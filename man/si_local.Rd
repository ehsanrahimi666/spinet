% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03-field.R
\name{si_local}
\alias{si_local}
\title{Extract the local network of one or more cells}
\usage{
si_local(field, cell, drop = TRUE, simplify = TRUE)
}
\arguments{
\item{field}{An [si_overlap()] field.}

\item{cell}{Integer vector of cell indices (positions within the field, not
raster cell numbers), or a two-column matrix / `SpatVector` of coordinates.}

\item{drop}{Logical. Remove species with no links from the returned matrix.
Network metrics are conventionally computed on the reduced matrix.}

\item{simplify}{Logical. When one cell is requested, return the matrix
itself rather than a list of length one.}
}
\value{
A matrix, or a named list of matrices.
}
\description{
Materialises the interaction matrix \eqn{W_k} for specific cells. This is
the only point at which a full local matrix exists in memory.
}
\examples{
set.seed(3)
f <- si_example_field()
w <- si_local(f, 5)
dim(w)
round(w[1:4, 1:4], 3)

# several cells at once
length(si_local(f, c(1, 2, 3)))
}
