% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07-turnover.R
\name{si_compare}
\alias{si_compare}
\title{Compare two fields metric by metric}
\usage{
si_compare(current, future, what = "all", cores = 1)
}
\arguments{
\item{current, future}{[si_field()] objects.}

\item{what}{Metrics to compute, passed to [si_metrics()].}

\item{cores}{Parallel workers.}
}
\value{
A `data.frame` of class `si_compare` with `_current`, `_future` and
  `_change` columns. The sign of `_change` is multiplied by the
  interaction's `overlap_sign`, so that for antagonistic or competitive
  networks a positive change consistently means deterioration.
}
\description{
Compare two fields metric by metric
}
\examples{
f1 <- si_example_field(n_cells = 50, seed = 1)
f2 <- f1; f2$A$values <- f2$A$values * 0.6; f2$scenario <- "future"
head(si_compare(f1, f2, what = c("links", "connectance", "NODF")), 3)
}
