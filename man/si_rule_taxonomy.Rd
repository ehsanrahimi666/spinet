% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06-forbidden.R
\name{si_rule_taxonomy}
\alias{si_rule_taxonomy}
\title{Forbidden links from taxonomic or functional specificity}
\usage{
si_rule_taxonomy(group_A, allowed, names_B, default = 1)
}
\arguments{
\item{group_A}{Character or factor vector giving each level-A species'
group (family, functional group).}

\item{allowed}{A named list mapping each level-B species to the groups it
can use, or a data frame with columns `species` and `group`.}

\item{names_B}{Character vector of level-B species names, in the order they
appear in the stack.}

\item{default}{Numeric 0 or 1. What to do for level-B species with no entry
in `allowed`. Default `1` (unconstrained).}
}
\value{
An `si_rule`.
}
\description{
Encodes host specificity: a consumer restricted to one plant family cannot
use resources outside it. Also used for functional-group aggregation.
}
\examples{
gA <- rep(c("Asteraceae", "Fabaceae", "Myrtaceae"), each = 4)
allowed <- list(bee1 = "Asteraceae", bee2 = c("Fabaceae", "Myrtaceae"))
si_rule_taxonomy(gA, allowed, names_B = c("bee1", "bee2", "fly1"))
}
