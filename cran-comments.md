## R CMD check results

0 errors | 0 warnings | 0 notes

* This is a new release.
* All exported functions have runnable examples; all examples execute in under
  five seconds each.
* `bipartite` and `igraph` are in Suggests and are used conditionally with
  `requireNamespace()`.
